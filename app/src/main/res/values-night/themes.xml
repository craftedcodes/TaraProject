<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.AbschlussAufgabe" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <item name="backgroundColor">@color/bg_dark</item>
        <item name="colorError">@color/error_bg_dark</item>
        <item name="colorPrimary">@color/primary_bg_dark</item>
        <item name="colorSecondary">@color/secondary_bg_dark</item>
        <item name="colorSurfaceContainer">@color/outline_bg_dark</item>
        <item name="colorErrorContainer">@color/error_c_bg_dark</item>

        <!-- Text Colors -->
        <item name="colorOnBackground">@color/bg_tx_dark</item>
        <item name="colorOnPrimary">@color/primary_tx_dark</item>
        <item name="colorOnSecondary">@color/secondary_tx_dark</item>
        <item name="colorOnError">@color/error_tx_dark</item>

        <!-- Container Background Colors -->
        <item name="colorSurface">@color/surface_c_bg_dark</item>
        <item name="colorOnSurface">@color/surface_c_tx_dark</item>
        <item name="colorOnPrimarySurface">@color/primary_c_tx_dark</item>
        <item name="colorOnSecondaryContainer">@color/secondary_c_tx_dark</item>
        <item name="android:statusBarColor">@color/bg_dark</item>
    </style>

    <style name="Theme.AbschlussAufgabe" parent="Base.Theme.AbschlussAufgabe" />
</resources>
